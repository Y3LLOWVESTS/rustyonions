[package]
publish = false
license = "MIT OR Apache-2.0"


name = "arti_transport"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = { workspace = true }
socks = "0.3"
tracing = "0.1"

# local crates
transport = { path = "../transport" }
accounting = { path = "../accounting" }

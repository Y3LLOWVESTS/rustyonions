[package]
publish = false
license = "MIT OR Apache-2.0"

name = "accounting"
version = "0.1.0"
edition = "2021"

[dependencies]
anyhow = "1"
parking_lot = "0.12"
workspace-hack = { version = "0.1", path = "../../workspace-hack" }

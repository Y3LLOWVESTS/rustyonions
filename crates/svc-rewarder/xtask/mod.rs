//! RO:WHAT — Placeholder xtask module for svc-rewarder crate-local automation.
//! RO:WHY — Pillar 12; Concerns: DX/GOV. Future CI checks can render diagrams and verify schemas.
//! RO:INTERACTS — docs/openapi, docs/mermaid, tests/vectors.
//! RO:INVARIANTS — automation must not mutate source files except generated artifacts.
//! RO:METRICS — none.
//! RO:CONFIG — none.
//! RO:SECURITY — no secrets.
//! RO:TEST — compile/read-only placeholder.

/// Batch-1 placeholder so the documented tree exists.
pub fn placeholder() -> &'static str {
    "svc-rewarder xtask placeholder"
}
